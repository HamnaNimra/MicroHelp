name: micro_help
description: MicroHelp – Neighborly micro-help platform built with Flutter and Firebase.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # Firebase
  firebase_core: ^4.4.0
  firebase_auth: ^6.1.4
  cloud_firestore: ^6.1.2
  firebase_messaging: ^16.1.1
  firebase_analytics: ^11.4.0
  firebase_crashlytics: ^4.3.2
  # Auth providers
  # google_sign_in: ^7.2.0  # Not supported on Windows desktop
  sign_in_with_apple: ^7.0.1
  # Location & misc
  geolocator: ^14.0.2
  # State management
  provider: ^6.1.2
  shared_preferences: ^2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
