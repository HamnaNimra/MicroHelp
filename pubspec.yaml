name: micro_help
description: MicroHelp – Neighborly micro-help platform built with Flutter and Firebase.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # Firebase
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  firebase_messaging: ^15.1.3
  # Auth providers
  google_sign_in: ^6.2.1
  sign_in_with_apple: ^6.1.2
  # Location & misc
  geolocator: ^13.0.1
  # State management
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
